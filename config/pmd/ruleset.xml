<?xml version="1.0"?>
<ruleset name="pi-java PMD Rules"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>PMD ruleset for pi-java project</description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <exclude name="UseVarargs"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="TooManyMethods"/>
        <exclude name="ShortMethodName"/>
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
        <exclude name="CommentDefaultAccessModifier"/>
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="CognitiveComplexity"/>
        <exclude name="CyclomaticComplexity"/>
        <exclude name="NcssCount"/>
        <exclude name="TooManyFields"/>
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <exclude name="CommentRequired"/>
        <exclude name="CommentSize"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="BeanMembersShouldSerialize"/>
        <exclude name="MissingSerialVersionUID"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml"/>

    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>

    <!-- Custom Rules -->
    <rule name="AvoidPrintStackTrace"
          language="java"
          message="Avoid printStackTrace(); use a logger instead"
          class="net.sourceforge.pmd.lang.rule.XPathRule">
        <description>Don't use printStackTrace(), use proper logging</description>
        <priority>2</priority>
        <properties>
            <property name="xpath" value="//PrimaryExpression[
                (PrimaryPrefix[Name[@Image='printStackTrace']] or 
                 PrimarySuffix[@Image='printStackTrace'])
            ]"/>
        </properties>
    </rule>

    <rule name="AvoidSystemOut"
          language="java"
          message="Avoid System.out; use a logger instead"
          class="net.sourceforge.pmd.lang.rule.XPathRule">
        <description>Don't use System.out, use proper logging</description>
        <priority>3</priority>
        <properties>
            <property name="xpath" value="//PrimaryExpression[
                PrimaryPrefix/Name[
                    starts-with(@Image, 'System.out')
                ]
            ]"/>
        </properties>
    </rule>
</ruleset>
